# 04 — 菜品池展示

**What to build:** 实现菜品池页面的只读展示。用户可通过横向 tab 切换分类，在每个分类下看到该分类的菜品列表，按添加时间倒序排列，禁用菜品置灰显示。

**Blocked by:** 03 — 云初始化 & 静默登录

**Status:** done

- [x] 菜品池页面顶部展示分类横向 tab，从 `user_config.categories` 读取分类列表
- [x] 切换 tab 加载对应分类的菜品列表（查询 `dishes` 集合按 `categoryId` 过滤）
- [x] 菜品按 `createdAt` 倒序排列
- [x] 禁用菜品（`enabled: false`）在列表中以置灰样式展示，与启用菜品视觉区分
- [x] 菜品卡片展示菜品名，若有图片则显示第一张图片缩略图
- [x] 无菜品时展示空状态占位（引导用户添加）
- [x] 页面加载时展示骨架屏或加载态
