<view class="empty-state">
  <image wx:if="{{icon}}" class="empty-icon" src="{{icon}}" mode="aspectFit" />
  <text class="empty-text">{{text}}</text>
  <slot />
  <view wx:if="{{actionText}}" class="empty-action" bind:tap="onAction">{{actionText}}</view>
</view>
